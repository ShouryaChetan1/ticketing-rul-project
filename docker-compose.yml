version: "3.9"

services:
  predimain:
    build: .
    container_name: predimain
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - model_data:/app/model_quick
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  model_data:
